WAL_TIMEZONE=Asia/Shanghai
WAL_DATA_DIR=./data
WAL_DB_PATH=./data/wechat_agent_lite.db
# 32-byte urlsafe base64 key recommended.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
WAL_ENCRYPTION_KEY=

